import { FC } from 'react'

import Meta from '@/ui/Meta'
import Button from '@/ui/button/button'

const Auth: FC = () => {
	return (
		<Meta title='Auth'>
			<Button>Auth</Button>
		</Meta>
	)
}

export default Auth
