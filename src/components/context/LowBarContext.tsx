// import { FC, RefObject, createContext, useContext, useRef } from 'react'

// export const LowBarContext = createContext<RefObject<any> | null>(null)

// export const LowBarProvider: FC<{ children: any }> = ({ children }) => {
// 	const refLowBar = useRef(null)

// 	return (
// 		<LowBarContext.Provider value={refLowBar}>
// 			{children}
// 		</LowBarContext.Provider>
// 	)
// }

// export const useLowBarContext = () => useContext(LowBarContext)
